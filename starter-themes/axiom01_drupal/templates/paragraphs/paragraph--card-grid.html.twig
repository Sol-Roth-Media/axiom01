{# paragraph--card-grid.html.twig #}
{% block paragraph %}
  <section{{ attributes }}>
    {{ title_prefix }}
    {{ title_suffix }}
    <div class="grid cols-3 gap-m" style="margin-top: var(--a-space-2xl);">
      {{ content.field_card_grid_items }}
    </div>
  </section>
{% endblock paragraph %}
