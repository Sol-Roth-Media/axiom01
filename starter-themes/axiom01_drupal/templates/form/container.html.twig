{#
/**
 * @file
 * Axiom01 override for form containers.
 *
 * Strips Drupal's wrapper div when not needed.
 */
#}
{% if has_parent %}
  {{ children }}
{% else %}
  <div{{ attributes }}>
    {{ children }}
  </div>
{% endif %}
