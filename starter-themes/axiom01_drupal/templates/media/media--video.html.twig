{#
/**
 * @file
 * Axiom01 override for Video media entities.
 */
#}
<figure{{ attributes.addClass('media', 'media--video') }}>
  {{ content.field_media_video_file }}
  {% if name and not name_is_hidden %}
    <figcaption>{{ name }}</figcaption>
  {% endif %}
</figure>
