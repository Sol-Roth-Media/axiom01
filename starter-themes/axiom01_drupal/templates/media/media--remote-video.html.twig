{#
/**
 * @file
 * Axiom01 override for Remote video media entities.
 */
#}
<figure{{ attributes.addClass('media', 'media--remote-video') }}>
  {{ content.field_media_oembed_video }}
  {% if name and not name_is_hidden %}
    <figcaption>{{ name }}</figcaption>
  {% endif %}
</figure>
