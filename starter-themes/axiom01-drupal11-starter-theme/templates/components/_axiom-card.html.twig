{# Reusable card partial for dashboard and listing UIs. #}
<article class="card">
  {% if title %}
    <header>
      <h3>{{ title }}</h3>
    </header>
  {% endif %}
  <div>
    {{ content }}
  </div>
</article>
