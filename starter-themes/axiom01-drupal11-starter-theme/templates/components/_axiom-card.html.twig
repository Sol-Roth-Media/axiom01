{# Reusable card partial for dashboard and listing UIs. #}
<section class="card">
  {% if title %}
    <header>
      <h3>{{ title }}</h3>
    </header>
  {% endif %}
  <main>
    {{ content }}
  </main>
  {% if footer %}
    <footer>
      {{ footer }}
    </footer>
  {% endif %}
</section>
