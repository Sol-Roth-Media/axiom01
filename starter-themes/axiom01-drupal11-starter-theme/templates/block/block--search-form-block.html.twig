{# Core search block wrapper mapped to Axiom search semantics. #}
{{ title_prefix }}
<section{{ attributes }}>
  {% if label %}
    <h2>{{ label }}</h2>
  {% endif %}
  {{ content }}
</section>
{{ title_suffix }}
