{# Starter-theme default search block wrapper with semantic search surface. #}
{{ title_prefix }}
<section{{ attributes }}>
  {% if label %}
    <h2>{{ label }}</h2>
  {% endif %}
  {{ content }}
</section>
{{ title_suffix }}
