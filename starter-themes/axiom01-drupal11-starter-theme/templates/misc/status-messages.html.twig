{# Accessible status messages mapped to alert semantics. #}
{% for type, messages in message_list %}
  {% set message_role = type == 'error' ? 'alert' : 'status' %}
  {% set message_live = type == 'error' ? 'assertive' : 'polite' %}
  {% set ax_type = type == 'status' ? 'success' : type|clean_class %}
  <section role="{{ message_role }}" aria-live="{{ message_live }}" aria-atomic="true" class="alert {{ ax_type }}" data-component="alert">
    {% if status_headings[type] %}
      <h2 class="visually-hidden">{{ status_headings[type] }}</h2>
    {% endif %}
    <ul>
      {% for message in messages %}
        <li>{{ message }}</li>
      {% endfor %}
    </ul>
  </section>
{% endfor %}
